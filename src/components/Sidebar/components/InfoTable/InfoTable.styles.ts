import styled from 'styled-components';

export const InfoTableContainer = styled.section`
    display: flex;
    align-items: center;
    justify-content: flex-start;
    flex-direction: column;
    margin-top: var(--spacing-m);
`;
