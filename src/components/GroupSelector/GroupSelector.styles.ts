import styled from 'styled-components';

export const GroupSelectorContainer = styled.section`
    display: flex;
    align-items: center;
    justify-content: flex-start;
    flex-wrap: wrap;
    width: 100rem;
    margin-top: var(--spacing-l);
`;
